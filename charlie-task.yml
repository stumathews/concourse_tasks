# does this comment work?
platform: linux
image_resource:
 type: docker-image
 source: {repository: ubuntu}
run:
 path: /bin/bash
 args:
 - -cel
 - |
   echo "This is charlie task  executing an inline, multiline bash script within a ubuntu container"

